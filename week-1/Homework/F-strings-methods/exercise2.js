const myName = " Daniel  ";
const nameLength = myName.length;
const message = `my name is ${myName}, it has ${nameLength} letters`;

console.log(message);
const trimedName = myName.trim();

const trimMessage = `my name is ${trimedName}, and my name is ${trimedName.length} characters long`;

console.log(trimMessage);
